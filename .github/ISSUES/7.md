# Automate Update of Major Version Tags

**yhakbar** commented *May 1, 2024*

The `v1` tag will track the latest `v1.y.z` version by force pushing the `v1` tag, etc.

Create a GitHub action that automates this process on release of a new tag.
<br />
***


