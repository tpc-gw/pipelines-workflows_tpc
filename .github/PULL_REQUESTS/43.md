# fix: Moving yml configurations for custom actions to an `action.yml` nested under the action name

**yhakbar** commented *Jul 1, 2024*

This is the file structure expected by GitHub Actions per the usage in `pipelines-root.yml`


<br />
***


