# feat: detect invalid read tokens

**ZachGoldberg** commented *Aug 12, 2024*

New feature:

Detect if we failed to checkout `pipelines-actions` and, if so, run a quick bash script that formats a message and outputs it both to the step summary, and posts a comment on the parent PR
<br />
***


